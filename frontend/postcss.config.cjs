module.exports = {
  // Vite + @tailwindcss/vite handle Tailwind v4 processing. Keep PostCSS minimal
  // to avoid double-processing. If your build requires PostCSS plugins later,
  // add them here.
  plugins: {},
};
